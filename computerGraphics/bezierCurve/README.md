# 2D Bézier Curve in C

This is a little projects developed in C dedicated to 2D SpLines studied in the course computer graphics.

## How to run

to run the code simply clone the repo and compile with gcc:

```bash
gcc -W -Wall <name>.c -o <execName>
```

then execute the compiled file with `./<execName>`.
